local colors = require("colors")

sbar.bar({
  color = colors.transparent,
  height = 32,
  margin = 8,
  corner_radius = 0,
  y_offset = 4,
  padding_left = 2,
  padding_right = 2,
  font_smoothing = "on",
})
